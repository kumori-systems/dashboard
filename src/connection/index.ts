import { state, StampState, Deployment, Manifest, Service, Version, Rol } from './logic';
import { getStampState, deploymentRolAddInstance, deploymentRolRemoveInstance } from './aux';

// Exportamos la lógica
export { state, StampState, Deployment, Manifest, Service, Version, Rol }

// Exportamos las funciones
export { getStampState, deploymentRolAddInstance, deploymentRolRemoveInstance }