import { ECloudElement } from './ecloudelement';
/**
 * A protocol in the ECloud ecosystem.
 */
export class Protocol extends ECloudElement {

  /**
   * A protocol in the ECloud ecosystem.
   */
  constructor() {
    super(ECloudElement.ECLOUDELEMENT_TYPE.PROTOCOL);
  }

}